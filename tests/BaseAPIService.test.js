import { shallowMount, mount, createLocalVue } from "@vue/test-utils";
import { render } from "@vue/server-test-utils"

describe("Base API", () => {
  beforeEach(() => {
    // store = new Vuex.Store({
    //   state: {}
    // })
  });

  it("makes call to endpoint", () => {

  });

  it("get JSON response", () => {

  });
});
